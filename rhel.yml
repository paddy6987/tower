- name: CREATE ARM Deployment PLAYBOOK
  hosts: localhost
  connection: local
  gather_facts: False
  
  tasks:
  - name: Create Azure Deploy
    azure_rm_deployment:
      state: present
      resource_group_name: dev-ops-cle
      template_link: 'https://raw.githubusercontent.com/paddy6987/tower/master/rhel.json'
      
        
